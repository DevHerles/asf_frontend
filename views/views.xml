<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="duplex_fe.list">
      <field name="name">duplex_fe list</field>
      <field name="model">duplex_fe.duplex_fe</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    
    <record model="ir.actions.act_window" id="duplex_fe.action_window">
      <field name="name">duplex_fe window</field>
      <field name="res_model">duplex_fe.teachers</field>
      <!-- <field name="view_mode">tree,form</field> -->
    </record>
   

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="duplex_fe.action_server">
      <field name="name">duplex_fe server</field>
      <field name="model_id" ref="model_duplex_fe_duplex_fe"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    
    <menuitem sequence="0" id="duplex_fe.menu_root" name="duplex_fe" />
   
    <!-- menu categories -->
    
    <menuitem name="Menu 1" id="duplex_fe.menu_1" parent="duplex_fe.menu_root"/>
    <!-- <menuitem name="Menu 2" id="duplex_fe.menu_2" parent="duplex_fe.menu_root"/> -->
   
    <!-- actions -->
    
    <menuitem name="List" id="duplex_fe.menu_1_list" parent="duplex_fe.menu_1"
              action="duplex_fe.action_window"/>
    <!-- <menuitem name="Server to list" id="duplex_fe" parent="duplex_fe.menu_2"
              action="duplex_fe.action_server"/> -->
   
  </data>
</odoo>